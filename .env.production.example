# ─── VORTEX Production Environment ─────────────────────
# Copy this file to .env and fill in your keys
# cp .env.production.example .env

# ─── Backend (Required) ────────────────────────────────

# Google Gemini API key (REQUIRED)
# Get yours at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Scheduler intervals (hours)
COLLECT_INTERVAL_HOURS=12
SOURCE_CHECK_INTERVAL_HOURS=1

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# CORS allowed origins (comma-separated)
# Replace YOUR_IP with your server's IP address
ALLOWED_ORIGINS=http://localhost,http://127.0.0.1,http://YOUR_IP

# ─── Frontend (Required for Docker build) ──────────────

# Clerk authentication key (REQUIRED)
# Get yours at: https://dashboard.clerk.com
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_clerk_key_here

# Backend API URL (REQUIRED)
# Replace YOUR_IP with your server's IP address
VITE_API_URL=http://YOUR_IP:8420
